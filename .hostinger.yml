language: php
php_version: "8.2"
build:
  commands:
    - |
      cat > .env << EOF
      APP_NAME=FlowCheck
      APP_ENV=production
      APP_KEY=base64:hfrtdo1BfzVyvF7nXxil9OcIvhaAX2G92BoEfAwnmi4=
      APP_DEBUG=false
      APP_URL=https://flowcheck.site
      LOG_CHANNEL=stack
      LOG_LEVEL=error
      DB_CONNECTION=mysql
      DB_HOST=127.0.0.1
      DB_PORT=3306
      DB_DATABASE=${DB_DATABASE}
      DB_USERNAME=${DB_USERNAME}
      DB_PASSWORD=${DB_PASSWORD}
      CACHE_DRIVER=file
      SESSION_DRIVER=file
      QUEUE_CONNECTION=database
      GOOGLE2FA_ENABLED=true
      SI68_COMPLIANCE_MODE=true
      CURRENCY_DEFAULT=ZMW
      EOF
